"""Generated by the Wheatear adapter foundry. Do not edit by hand.

Regenerating from the same MappingSpec produces identical output, so an edit
here is lost the next time the corridor is rebuilt. Change the spec instead.

  platform   copilot-studio
  direction  import   (platform record -> Wheatear IR)
  entity     tool
  schema     435c6a63ad313e79
  generator  GoogleProvider

Contract:
  transform(record) -> dict   maps one source record onto the target shape.
                              Pure. Never raises. Omits any target field whose
                              source was absent rather than writing a null.
  flags(record)     -> list   per-record review notes: fields present in this
                              record that could not be carried across.
"""

ADAPTER_META = {'platform': 'copilot-studio', 'direction': 'import', 'entity_kind': 'tool', 'schema_fingerprint': '435c6a63ad313e79bc075911843ae62aae99ff0a3dd3da71d486c7785961877c', 'ir_version': 'wheatear/v1', 'spec_version': 'wheatear.foundry/v1', 'mappings': 11}

# Static review flags from the mapping spec: what this adapter cannot carry for
# any record. Per-record findings come from `flags()`.
REVIEW_FLAGS = [{'path': 'bridge', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `bridge`; it will be left at its default.', 'severity': 'info'}, {'path': 'catalog_connections[]', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `catalog_connections[]`; it will be left at its default.', 'severity': 'info'}, {'path': 'catalog_title', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `catalog_title`; it will be left at its default.', 'severity': 'info'}, {'path': 'confidence', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `confidence`; it will be left at its default.', 'severity': 'info'}, {'path': 'inputs[].type', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `inputs[].type`; it will be left at its default.', 'severity': 'info'}, {'path': 'mcp_server_url', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `mcp_server_url`; it will be left at its default.', 'severity': 'info'}, {'path': 'member_tools[]', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `member_tools[]`; it will be left at its default.', 'severity': 'info'}, {'path': 'notes', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `notes`; it will be left at its default.', 'severity': 'info'}, {'path': 'outputs[].type', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `outputs[].type`; it will be left at its default.', 'severity': 'info'}, {'path': 'review_required', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `review_required`; it will be left at its default.', 'severity': 'info'}, {'path': 'source_ref', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `source_ref`; it will be left at its default.', 'severity': 'info'}, {'path': 'transport', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `transport`; it will be left at its default.', 'severity': 'info'}, {'path': 'botcomponent.@schemaname', 'reason': 'lossy', 'detail': '`botcomponent.@schemaname` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'botcomponent.componenttype', 'reason': 'lossy', 'detail': '`botcomponent.componenttype` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'botcomponent.iscustomizable', 'reason': 'lossy', 'detail': '`botcomponent.iscustomizable` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'botcomponent.parentbotid.schemaname', 'reason': 'lossy', 'detail': '`botcomponent.parentbotid.schemaname` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'botcomponent.statecode', 'reason': 'lossy', 'detail': '`botcomponent.statecode` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'botcomponent.statuscode', 'reason': 'lossy', 'detail': '`botcomponent.statuscode` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.action.connectionProperties.mode', 'reason': 'lossy', 'detail': '`data.action.connectionProperties.mode` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.action.connectionReference', 'reason': 'lossy', 'detail': '`data.action.connectionReference` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.action.dynamicOutputSchema.kind', 'reason': 'lossy', 'detail': '`data.action.dynamicOutputSchema.kind` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.action.dynamicOutputSchema.properties.result.order', 'reason': 'lossy', 'detail': '`data.action.dynamicOutputSchema.properties.result.order` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.action.dynamicOutputSchema.properties.result.type', 'reason': 'lossy', 'detail': '`data.action.dynamicOutputSchema.properties.result.type` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.inputs[].kind', 'reason': 'lossy', 'detail': '`data.inputs[].kind` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'data.kind', 'reason': 'lossy', 'detail': '`data.kind` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.modelDisplayName', 'reason': 'lossy', 'detail': '`data.modelDisplayName` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.outputMode', 'reason': 'lossy', 'detail': '`data.outputMode` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.outputs[].propertyName', 'reason': 'lossy', 'detail': '`data.outputs[].propertyName` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'catalog_install_ref', 'reason': 'semantic_ambiguity', 'detail': "`catalog_install_ref` is derived rather than carried over: The target field can be derived by slugifying the source 'botcomponent.name', for example by lowercasing and replacing spaces with underscores.", 'severity': 'warn'}, {'path': 'connector_id', 'reason': 'semantic_ambiguity', 'detail': "`connector_id` is derived rather than carried over: The connector ID can be derived by parsing the prefix from 'botcomponent.name', such as taking 'ServiceNow' from 'ServiceNow - Get Record'.", 'severity': 'warn'}]

_MISSING = object()


def _get(record, path):
    """Read a dotted path. `[]` maps across an array rather than indexing it."""
    if not path:
        return record
    current = record
    for segment in path.split("."):
        collect = segment.endswith("[]")
        key = segment[:-2] if collect else segment
        if key:
            if isinstance(current, list):
                current = [i.get(key) for i in current if isinstance(i, dict)]
            elif isinstance(current, dict):
                if key not in current:
                    return _MISSING
                current = current[key]
            else:
                return _MISSING
        if collect:
            if not isinstance(current, list):
                return _MISSING
            flat = []
            for item in current:
                flat.extend(item) if isinstance(item, list) else flat.append(item)
            current = flat
    return current


def _put(target, path, value):
    """Write a dotted path, creating intermediate dicts."""
    parts = path.split(".")
    node = target
    for key in parts[:-1]:
        step = node.get(key)
        if not isinstance(step, dict):
            step = {}
            node[key] = step
        node = step
    node[parts[-1]] = value


def _text(value):
    """The faithful string for a value, or _MISSING when there isn't one."""
    if value is None:
        return _MISSING
    if isinstance(value, str):
        return value
    if isinstance(value, bool):
        return "true" if value else "false"
    if isinstance(value, (int, float)):
        return str(value)
    if isinstance(value, (list, tuple)):
        parts = [_text(v) for v in value]
        kept = [p for p in parts if p is not _MISSING]
        return ", ".join(kept) if kept else _MISSING
    # A dict has no honest flat rendering; refusing beats emitting a repr.
    return _MISSING


def _number(value, want_int):
    if isinstance(value, bool) or value is None:
        return _MISSING
    if isinstance(value, (int, float)):
        return int(value) if want_int else float(value)
    if isinstance(value, str):
        try:
            return int(value.strip()) if want_int else float(value.strip())
        except ValueError:
            return _MISSING
    return _MISSING


def _boolean(value):
    if isinstance(value, bool):
        return value
    if isinstance(value, str):
        lowered = value.strip().lower()
        if lowered in ("true", "yes", "1", "on"):
            return True
        if lowered in ("false", "no", "0", "off"):
            return False
    if isinstance(value, (int, float)):
        return bool(value)
    return _MISSING


def _listed(value):
    if value is None:
        return _MISSING
    if isinstance(value, list):
        return value
    if isinstance(value, tuple):
        return list(value)
    return [value]


def _coerce(value, want):
    """Convert to the target's declared type, or _MISSING if it can't be done."""
    if want == "array":
        return _listed(value)
    if want == "string":
        return _text(value)
    if want == "integer":
        return _number(value, True)
    if want == "number":
        return _number(value, False)
    if want == "boolean":
        return _boolean(value)
    return value if value is not None else _MISSING


# Written by the Engineer for `catalog_install_ref`.
def _derive_catalog_install_ref(record):
    import re
    name = _get(record, 'botcomponent.name')
    if not isinstance(name, str) or not name:
        return _MISSING
    slug = re.sub(r'[^a-z0-9]+', '_', name.lower()).strip('_')
    return slug


# Written by the Engineer for `connector_id`.
def _derive_connector_id(record):
    name = _get(record, 'botcomponent.name')
    if not isinstance(name, str) or not name:
        return _MISSING
    return name.split(' - ', 1)[0]


# Written by the Engineer for `outputs[].description`.
def _derive_outputs___description(record, source_item, **kwargs):
    if not isinstance(source_item, dict):
        return _MISSING
    return _get(source_item, 'description')


def transform(record):
    """Map one source record onto the target shape."""
    if not isinstance(record, dict):
        return {}
    out = {}

    # catalog_artifact_id <- data.action.operationId  [copy, confidence 0.90]
    _v = _get(record, 'data.action.operationId')
    if _v is not _MISSING and _v is not None:
        _put(out, 'catalog_artifact_id', _v)

    # description <- data.modelDescription  [copy, confidence 1.00]
    _v = _get(record, 'data.modelDescription')
    if _v is not _MISSING and _v is not None:
        _put(out, 'description', _v)

    # kind <- data.action.kind  [enum_map, confidence 1.00]
    _v = _get(record, 'data.action.kind')
    if _v is not _MISSING:
        _v = {'InvokeConnectorTaskAction': 'connector'}.get(_text(_v), _MISSING)
    if _v is not _MISSING and _v is not None:
        _put(out, 'kind', _v)

    # operation_id <- data.action.operationId  [rename, confidence 0.90]
    _v = _get(record, 'data.action.operationId')
    if _v is not _MISSING and _v is not None:
        _put(out, 'operation_id', _v)

    # ref <- data.action.operationId  [copy, confidence 1.00]
    _v = _get(record, 'data.action.operationId')
    if _v is not _MISSING and _v is not None:
        _put(out, 'ref', _v)

    # inputs[] <- data.inputs[]  [collect, 2 field(s)]
    _items = _get(record, 'data.inputs[]')
    if isinstance(_items, list):
        _rows = []
        for _item in _items:
            _row = {}
            _v = _get(_item, 'description')
            if _v is not _MISSING and _v is not None:
                _put(_row, 'description', _v)
            _v = _get(_item, 'propertyName')
            if _v is not _MISSING and _v is not None:
                _put(_row, 'name', _v)
            _rows.append(_row)
        _put(out, 'inputs', _rows)

    # outputs[] <- data.outputs[]  [collect, 1 field(s)]
    _items = _get(record, 'data.outputs[]')
    if isinstance(_items, list):
        _rows = []
        for _item in _items:
            _row = {}
            _v = _get(_item, 'name')
            if _v is not _MISSING and _v is not None:
                _put(_row, 'name', _v)
            _rows.append(_row)
        _put(out, 'outputs', _rows)

    # catalog_install_ref <- derived
    try:
        _v = _derive_catalog_install_ref(record)
    except Exception:
        _v = _MISSING
    if _v is not _MISSING and _v is not None:
        _put(out, 'catalog_install_ref', _v)

    # connector_id <- derived
    try:
        _v = _derive_connector_id(record)
    except Exception:
        _v = _MISSING
    if _v is not _MISSING and _v is not None:
        _put(out, 'connector_id', _v)

    # outputs[].description <- derived
    try:
        _v = _derive_outputs___description(record)
    except Exception:
        _v = _MISSING
    if _v is not _MISSING and _v is not None:
        _put(out, 'outputs[].description', _v)

    # Unfilled IR fields, set to their type's empty value so that one
    # key means one thing whatever platform the record came from.
    if _get(out, 'catalog_title') is _MISSING:
        _put(out, 'catalog_title', '')
    if _get(out, 'confidence') is _MISSING:
        _put(out, 'confidence', 0)
    if _get(out, 'mcp_server_url') is _MISSING:
        _put(out, 'mcp_server_url', '')
    if _get(out, 'notes') is _MISSING:
        _put(out, 'notes', '')
    if _get(out, 'review_required') is _MISSING:
        _put(out, 'review_required', False)
    if _get(out, 'source_ref') is _MISSING:
        _put(out, 'source_ref', '')
    if _get(out, 'transport') is _MISSING:
        _put(out, 'transport', '')

    return out


def flags(record):
    """Review notes for this specific record."""
    found = []
    if not isinstance(record, dict):
        return found
    _v = _get(record, 'botcomponent.@schemaname')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.@schemaname',
            'reason': 'lossy',
            'detail': '`botcomponent.@schemaname` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'botcomponent.componenttype')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.componenttype',
            'reason': 'lossy',
            'detail': '`botcomponent.componenttype` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'botcomponent.iscustomizable')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.iscustomizable',
            'reason': 'lossy',
            'detail': '`botcomponent.iscustomizable` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'botcomponent.parentbotid.schemaname')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.parentbotid.schemaname',
            'reason': 'lossy',
            'detail': '`botcomponent.parentbotid.schemaname` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'botcomponent.statecode')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.statecode',
            'reason': 'lossy',
            'detail': '`botcomponent.statecode` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'botcomponent.statuscode')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.statuscode',
            'reason': 'lossy',
            'detail': '`botcomponent.statuscode` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.action.connectionProperties.mode')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.action.connectionProperties.mode',
            'reason': 'lossy',
            'detail': '`data.action.connectionProperties.mode` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.action.connectionReference')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.action.connectionReference',
            'reason': 'lossy',
            'detail': '`data.action.connectionReference` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.action.dynamicOutputSchema.kind')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.action.dynamicOutputSchema.kind',
            'reason': 'lossy',
            'detail': '`data.action.dynamicOutputSchema.kind` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.action.dynamicOutputSchema.properties.result.order')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.action.dynamicOutputSchema.properties.result.order',
            'reason': 'lossy',
            'detail': '`data.action.dynamicOutputSchema.properties.result.order` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.action.dynamicOutputSchema.properties.result.type')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.action.dynamicOutputSchema.properties.result.type',
            'reason': 'lossy',
            'detail': '`data.action.dynamicOutputSchema.properties.result.type` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.kind')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.kind',
            'reason': 'lossy',
            'detail': '`data.kind` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.modelDisplayName')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.modelDisplayName',
            'reason': 'lossy',
            'detail': '`data.modelDisplayName` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.outputMode')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.outputMode',
            'reason': 'lossy',
            'detail': '`data.outputMode` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.outputs[].propertyName')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.outputs[].propertyName',
            'reason': 'lossy',
            'detail': '`data.outputs[].propertyName` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    return found
