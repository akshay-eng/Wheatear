"""Generated by the Wheatear adapter foundry. Do not edit by hand.

Regenerating from the same MappingSpec produces identical output, so an edit
here is lost the next time the corridor is rebuilt. Change the spec instead.

  platform   copilot-studio
  direction  import   (platform record -> Wheatear IR)
  entity     topic
  schema     435c6a63ad313e79
  generator  GoogleProvider

Contract:
  transform(record) -> dict   maps one source record onto the target shape.
                              Pure. Never raises. Omits any target field whose
                              source was absent rather than writing a null.
  flags(record)     -> list   per-record review notes: fields present in this
                              record that could not be carried across.
"""

ADAPTER_META = {'platform': 'copilot-studio', 'direction': 'import', 'entity_kind': 'topic', 'schema_fingerprint': '435c6a63ad313e79bc075911843ae62aae99ff0a3dd3da71d486c7785961877c', 'ir_version': 'wheatear/v1', 'spec_version': 'wheatear.foundry/v1', 'mappings': 3}

# Static review flags from the mapping spec: what this adapter cannot carry for
# any record. Per-record findings come from `flags()`.
REVIEW_FLAGS = [{'path': 'unsupported_notes[]', 'reason': 'no_target_equivalent', 'detail': 'No source field produces `unsupported_notes[]`; it will be left at its default.', 'severity': 'info'}, {'path': 'botcomponent.@schemaname', 'reason': 'lossy', 'detail': '`botcomponent.@schemaname` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'botcomponent.componenttype', 'reason': 'lossy', 'detail': '`botcomponent.componenttype` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'botcomponent.description', 'reason': 'lossy', 'detail': '`botcomponent.description` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'botcomponent.iscustomizable', 'reason': 'lossy', 'detail': '`botcomponent.iscustomizable` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'botcomponent.parentbotid.schemaname', 'reason': 'lossy', 'detail': '`botcomponent.parentbotid.schemaname` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'botcomponent.statecode', 'reason': 'lossy', 'detail': '`botcomponent.statecode` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'botcomponent.statuscode', 'reason': 'lossy', 'detail': '`botcomponent.statuscode` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.beginDialog.actions[].*.allowInterruption', 'reason': 'lossy', 'detail': '`data.beginDialog.actions[].*.allowInterruption` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'data.beginDialog.actions[].*.items', 'reason': 'lossy', 'detail': '`data.beginDialog.actions[].*.items` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'data.beginDialog.actions[].*.kind', 'reason': 'lossy', 'detail': '`data.beginDialog.actions[].*.kind` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'data.beginDialog.actions[].*.speak', 'reason': 'lossy', 'detail': '`data.beginDialog.actions[].*.speak` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'data.beginDialog.actions[].*.text', 'reason': 'lossy', 'detail': '`data.beginDialog.actions[].*.text` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'data.beginDialog.id', 'reason': 'lossy', 'detail': '`data.beginDialog.id` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.beginDialog.intent.displayName', 'reason': 'lossy', 'detail': '`data.beginDialog.intent.displayName` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'data.beginDialog.intent.includeInOnSelectIntent', 'reason': 'lossy', 'detail': '`data.beginDialog.intent.includeInOnSelectIntent` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'data.beginDialog.priority', 'reason': 'lossy', 'detail': '`data.beginDialog.priority` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'data.beginDialog.triggerBehavior', 'reason': 'lossy', 'detail': '`data.beginDialog.triggerBehavior` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'data.kind', 'reason': 'lossy', 'detail': '`data.kind` has no counterpart on the target and is dropped.', 'severity': 'warn'}, {'path': 'data.startBehavior', 'reason': 'lossy', 'detail': '`data.startBehavior` has no counterpart on the target and is dropped.', 'severity': 'info'}, {'path': 'is_system_topic', 'reason': 'semantic_ambiguity', 'detail': "`is_system_topic` is derived rather than carried over: The boolean `is_system_topic` can be derived from `data.beginDialog.kind`, as values like 'OnError' or 'OnSignIn' represent system-level triggers.", 'severity': 'warn'}]

_MISSING = object()


def _get(record, path):
    """Read a dotted path. `[]` maps across an array rather than indexing it."""
    if not path:
        return record
    current = record
    for segment in path.split("."):
        collect = segment.endswith("[]")
        key = segment[:-2] if collect else segment
        if key:
            if isinstance(current, list):
                current = [i.get(key) for i in current if isinstance(i, dict)]
            elif isinstance(current, dict):
                if key not in current:
                    return _MISSING
                current = current[key]
            else:
                return _MISSING
        if collect:
            if not isinstance(current, list):
                return _MISSING
            flat = []
            for item in current:
                flat.extend(item) if isinstance(item, list) else flat.append(item)
            current = flat
    return current


def _put(target, path, value):
    """Write a dotted path, creating intermediate dicts."""
    parts = path.split(".")
    node = target
    for key in parts[:-1]:
        step = node.get(key)
        if not isinstance(step, dict):
            step = {}
            node[key] = step
        node = step
    node[parts[-1]] = value


def _text(value):
    """The faithful string for a value, or _MISSING when there isn't one."""
    if value is None:
        return _MISSING
    if isinstance(value, str):
        return value
    if isinstance(value, bool):
        return "true" if value else "false"
    if isinstance(value, (int, float)):
        return str(value)
    if isinstance(value, (list, tuple)):
        parts = [_text(v) for v in value]
        kept = [p for p in parts if p is not _MISSING]
        return ", ".join(kept) if kept else _MISSING
    # A dict has no honest flat rendering; refusing beats emitting a repr.
    return _MISSING


def _number(value, want_int):
    if isinstance(value, bool) or value is None:
        return _MISSING
    if isinstance(value, (int, float)):
        return int(value) if want_int else float(value)
    if isinstance(value, str):
        try:
            return int(value.strip()) if want_int else float(value.strip())
        except ValueError:
            return _MISSING
    return _MISSING


def _boolean(value):
    if isinstance(value, bool):
        return value
    if isinstance(value, str):
        lowered = value.strip().lower()
        if lowered in ("true", "yes", "1", "on"):
            return True
        if lowered in ("false", "no", "0", "off"):
            return False
    if isinstance(value, (int, float)):
        return bool(value)
    return _MISSING


def _listed(value):
    if value is None:
        return _MISSING
    if isinstance(value, list):
        return value
    if isinstance(value, tuple):
        return list(value)
    return [value]


def _coerce(value, want):
    """Convert to the target's declared type, or _MISSING if it can't be done."""
    if want == "array":
        return _listed(value)
    if want == "string":
        return _text(value)
    if want == "integer":
        return _number(value, True)
    if want == "number":
        return _number(value, False)
    if want == "boolean":
        return _boolean(value)
    return value if value is not None else _MISSING


# Written by the Engineer for `is_system_topic`.
def _derive_is_system_topic(record, M):
    kind = _get(record, "data.beginDialog.kind")
    if kind is _MISSING:
        return _MISSING
    system_trigger_kinds = {
        "OnConversationUpdateActivity",
        "OnEndOfConversationActivity",
        "OnError",
        "OnHandoffActivity",
        "OnSignIn",
        "OnUnknownIntent",
    }
    return kind in system_trigger_kinds


def transform(record):
    """Map one source record onto the target shape."""
    if not isinstance(record, dict):
        return {}
    out = {}

    # name <- botcomponent.name  [rename, confidence 0.90]
    _v = _get(record, 'botcomponent.name')
    if _v is not _MISSING and _v is not None:
        _put(out, 'name', _v)

    # trigger_phrases <- data.beginDialog.intent.triggerQueries[]  [copy, confidence 1.00]
    _v = _get(record, 'data.beginDialog.intent.triggerQueries[]')
    if _v is not _MISSING and _v is not None:
        _put(out, 'trigger_phrases', _v)

    # is_system_topic <- derived
    try:
        _v = _derive_is_system_topic(record)
    except Exception:
        _v = _MISSING
    if _v is not _MISSING and _v is not None:
        _put(out, 'is_system_topic', _v)

    return out


def flags(record):
    """Review notes for this specific record."""
    found = []
    if not isinstance(record, dict):
        return found
    _v = _get(record, 'botcomponent.@schemaname')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.@schemaname',
            'reason': 'lossy',
            'detail': '`botcomponent.@schemaname` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'botcomponent.componenttype')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.componenttype',
            'reason': 'lossy',
            'detail': '`botcomponent.componenttype` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'botcomponent.iscustomizable')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.iscustomizable',
            'reason': 'lossy',
            'detail': '`botcomponent.iscustomizable` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'botcomponent.parentbotid.schemaname')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.parentbotid.schemaname',
            'reason': 'lossy',
            'detail': '`botcomponent.parentbotid.schemaname` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'botcomponent.statecode')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.statecode',
            'reason': 'lossy',
            'detail': '`botcomponent.statecode` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'botcomponent.statuscode')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'botcomponent.statuscode',
            'reason': 'lossy',
            'detail': '`botcomponent.statuscode` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.beginDialog.id')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.beginDialog.id',
            'reason': 'lossy',
            'detail': '`data.beginDialog.id` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    _v = _get(record, 'data.kind')
    if _v is not _MISSING and _v not in (None, '', [], {}):
        found.append({
            'path': 'data.kind',
            'reason': 'lossy',
            'detail': '`data.kind` has no counterpart on the target and is dropped.',
            'severity': 'warn',
        })
    return found
